endpoints:
  runray.xyz:
    hostname: "*.runray.xyz"
    port: 443
    protocol: HTTPS
    tls:
      externalSecretNames:
        - "liferay-certificates-runray.xyz"

# license:
#   externalSecretName: "liferay-licenses-developer-clustered"
#   targetSecretName: "developer-clustered"

# overlayAccess:
#   enabled: true

# DXP 2026.q3.0 requires OpenSearch 2.19 or later. The chart still provisions
# 2.15, which the search engine rejects at startup.
search:
  opensearch:
    engineVersion: OpenSearch_2.19

# enable below on GCP
#search:
#  elasticsearch:
#    version: 8.17.4
